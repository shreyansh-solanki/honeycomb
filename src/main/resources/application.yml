server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: hcomb
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5432/hcomb
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true